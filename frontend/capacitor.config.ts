import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.piraterace',
  appName: 'PirateRace',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
